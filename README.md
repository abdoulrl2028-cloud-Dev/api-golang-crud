# api-golang-crud
# API REST em Golang (CRUD)  API REST criada em Go com operações CRUD e estruturação em camadas (handler/service/repository).  ## Tecnologias - Go - Gin (HTTP) - PostgreSQL (opcional) - Docker (opcional)  ## Rotas - POST /users - GET /users - GET /users/:id - PUT /users/:id - DELETE /users/:id  ## Como rodar ```bash go run ./cmd/api
